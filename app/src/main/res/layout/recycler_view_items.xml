<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ListingConstrainLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/recyclerImageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        android:scaleType="centerInside"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/mountain" />

    <TextView
        android:id="@+id/destinationtextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:text="TextView"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.473"
        app:layout_constraintStart_toEndOf="@+id/recyclerImageView"
        app:layout_constraintTop_toTopOf="@+id/recyclerImageView"
        tools:text="Las Vegas" />

    <TextView
        android:id="@+id/dLocationTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="TextView"
        android:textAppearance="@style/TextAppearance.AppCompat.Small"
        app:layout_constraintEnd_toEndOf="@+id/destinationtextView"
        app:layout_constraintStart_toStartOf="@+id/destinationtextView"
        app:layout_constraintTop_toBottomOf="@+id/destinationtextView"
        tools:text="Las Vegas Holiday Resort" />

    <TextView
        android:id="@+id/vacationPrice"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:gravity="end"
        android:text="TextView"
        android:textAlignment="viewEnd"
        android:textColor="@color/priceColor"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/recyclerImageView"
        app:layout_constraintEnd_toEndOf="@+id/dLocationTextView"
        app:layout_constraintStart_toStartOf="@+id/dLocationTextView"
        app:layout_constraintTop_toBottomOf="@+id/dLocationTextView"
        tools:text="150000" />
</android.support.constraint.ConstraintLayout>